export enum RabbitMQ {
  CanalQueue = 'canals',
}

export enum CanalMsg {
  CREATE = 'CREATE_CANAL',
  FIND_ALL = 'FIND_CANALS',
  FIND_ONE = 'FIND_CANAL',
  UPDATE = 'UPDATE_CANAL',
  DELETE = 'DELETE_CANAL',
}
